function make-nchars() {
	[ -n "$1" ] || return 1

	grep -h "^$1" data/*.tsv |\
	awk -F$'\t' '{print $5;}' |\
	sed -r -e 's!https?://[-_a-zA-Z0-9./?%]+!!g' -e 's/^\s+|\s+$//g' |\
	perl -CS -ne 'chomp;print length($_),"\n"' |\
	sort -n
}
